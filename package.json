{
  "name": "openclaw-nextcloud",
  "version": "0.2.5",
  "description": "Manage Notes, Tasks, Calendar, Files, Contacts, Shares, and Talk conversations in your Nextcloud instance via CalDAV, WebDAV, CardDAV, Notes API, OCS Shares, and Talk API.",
  "bin": {
    "nextcloud": "./scripts/nextcloud.js"
  },
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --outfile=scripts/nextcloud.js --format=esm",
    "test": "node --test tests/**/*.test.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "fast-xml-parser": "^5.3.4"
  },
  "devDependencies": {
    "esbuild": "0.27.7"
  }
}
